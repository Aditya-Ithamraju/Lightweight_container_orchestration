#!/usr/bin/env bash
# High-level steps automated minimally:
echo "1) Deploy an app and note which node it landed on"
echo "2) Stop that node VM (or network) -> e.g. multipass stop edge1"
echo "3) Re-deploy and verify it goes to another node"
